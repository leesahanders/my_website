---
title: "Examples for connecting to Snowflake using Managed Credentials"
description: "Just examples"
author: "Lisa"
date: "2026-03-23"
format:
  html:
    code-fold: true
    code-summary: "Show the code"
editor: source
toc: true
image: "img/"
draft: false
freeze: true
filters:
   - lightbox
lightbox: auto
categories:
  - Posit
---

Check that credentials are cached in your session with this command, run in terminal: `env | grep SNOWFLAKE`


Here is an example from <https://docs.posit.co/ide/server-pro/user/posit-workbench/managed-credentials/snowflake.html>
The Snowflake CLI wraps Snowflake SQL and provides configuration management and data access tools.

```
# List connections
snow connection list
```

And then here is an easy one for R: 

```
library(DBI)
library(odbc)

conn <- DBI::dbConnect(odbc::snowflake())
DBI::dbGetQuery(conn, "SELECT CURRENT_USER()")
```

Here is the Shiny app (save as an app.R file, you'll also need to change WAREHOUSE, DB, and SCHEMA and add SNOWFLAKE_ACCOUNT as an environment variable and add the snowflake integration): 

```r

library(shiny)
library(bslib)
library(DBI)
library(odbc)
library(dplyr)
library(connectcreds)

ui <- page_fluid(
  title = "Snowflake test",
  card(
    card_header("who am i"),
    verbatimTextOutput("whoami")
  ),
  card(
    card_header("connection details"),
    verbatimTextOutput("connection_info")
  ),
  card(
    card_header("data"),
    tableOutput("data")
  )
)

server <- function(input, output, session) {
 
  # Define connection parameters
  WAREHOUSE <- "DEFAULT_WH"
  DB <- "LENDING_CLUB"
  SCHEMA <- "PUBLIC"
  ACCOUNT <- Sys.getenv("SNOWFLAKE_ACCOUNT")
 
  con <- DBI::dbConnect(odbc::snowflake(),
                        # connection_name = "workbench",
                        warehouse = WAREHOUSE,
                        account = ACCOUNT,
                        database = DB,
                        schema = SCHEMA
  )
 
 
  output$whoami <- renderText ({
    whoami <- (
      DBI::dbGetQuery(con, 'SELECT CURRENT_USER();')
    )
    userrole <- (
      DBI::dbGetQuery(con, 'SELECT CURRENT_ROLE();')
    )
    paste0("Hello, ", whoami, ". Your role is: ",userrole)
  })
 
  output$connection_info <- renderText({
    con_info <- paste(capture.output(print(con)), collapse = "\n")
    paste0(
      "Warehouse: ", WAREHOUSE, "\n",
      "Account: ", ACCOUNT, "\n",
      "Database: ", DB, "\n",
      "Schema: ", SCHEMA, "\n\n",
      "Connection Object:\n", con_info
    )
  })
 
  output$data <- renderTable ({
    DBI::dbGetQuery(con, "SHOW TABLES;")
  })
}

shinyApp(ui, server)
```

Here is the Quarto report (you'll need to change WAREHOUSE, DB, and SCHEMA, and note that this won't run on Connect since it won't be able to interactively access credentials): 

````r
---
title: "test"
format: html
editor: source
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```r
1 + 1
```

You can add options to executable code like this

```r
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).

## Make a connection to snowflake

Show the credentials with:

`env | grep SNOWFLAKE`

```r
  # Define connection parameters
  WAREHOUSE <- "DEFAULT_WH"
  DB <- "LENDING_CLUB"
  SCHEMA <- "PUBLIC"
  ACCOUNT <- Sys.getenv("SNOWFLAKE_ACCOUNT")
 
  con <- DBI::dbConnect(odbc::snowflake(),
                        # connection_name = "workbench",
                        warehouse = WAREHOUSE,
                        account = ACCOUNT,
                        database = DB,
                        schema = SCHEMA
  )
```

## Results


```r
whoami <- (
  DBI::dbGetQuery(con, 'SELECT CURRENT_USER();')
)
userrole <- (
  DBI::dbGetQuery(con, 'SELECT CURRENT_ROLE();')
)
paste0("Hello, ", whoami, ". Your role is: ",userrole)
```

```r
DBI::dbGetQuery(con, "SHOW TABLES;")
```
````

Last here is a python example (from <https://docs.posit.co/ide/server-pro/user/posit-workbench/managed-credentials/snowflake.html>)


```bash
pip install snowflake-connector-python
```

```py
import snowflake.connector

with snowflake.connector.connect() as conn:
    with conn.cursor() as cur:
        print(cur.execute("SELECT 'successfully connected to ' || current_account() || ' WITH user ' || current_user();").fetchall())
```


```bash
python3 test_connection.py
```




